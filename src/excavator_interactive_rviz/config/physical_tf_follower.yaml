/**:
  ros__parameters:
    # ── Behaviour ─────────────────────────────────────────────────────────
    mirror_to_joint_states:   true
    publish_world_to_base_tf: true
    publish_rate:             30.0

    # ── Frame names — raw kinematic IDs from Novatron/rosbag ──────────────
    frame_world_offset:  "world_offset"
    frame_body_6dof:     "kinematic_1005"
    frame_body_yaw:      "kinematic_1000"
    frame_boom:          "kinematic_1003"
    frame_stick:         "kinematic_1002"
    frame_bucket:        "kinematic_2001"

    # ── Signs — confirmed from TF dump analysis ───────────────────────────
    body_sign:   1.0
    boom_sign:   1.0
    stick_sign:  1.0
    bucket_sign: 1.0

    # ── Roll correction for world→base_link ───────────────────────────────
    base_link_roll_correction: 0.0
