# Trajectory execution for MoveIt -> arm_trajectory_controller (Gazebo / ros2_control)
# Keys under trajectory_execution become trajectory_execution.* on move_group (required in MoveIt 2).
trajectory_execution:
  allowed_execution_duration_scaling: 2.0
  allowed_goal_duration_margin: 0.5
  # Default was 0.01 rad — too tight vs Gazebo joint_state noise / continuous joints.
  allowed_start_tolerance: 0.25

moveit_controller_manager: moveit_simple_controller_manager/MoveItSimpleControllerManager

moveit_simple_controller_manager:
  controller_names:
    - arm_trajectory_controller

  arm_trajectory_controller:
    type: FollowJointTrajectory
    action_ns: follow_joint_trajectory
    default: true
    joints:
      - body_rotation
      - arm1_rotation
      - arm2_rotation
      - shovel_rotation
